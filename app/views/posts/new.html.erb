<h1> New Tweet</h1>

<%= form_for @post do |f|  %>
	<p>
		<%= f.label :title %><br />
		<%= f.text_field :title %><br />

		<%= f.label :content %><br />
		<%= f.text_area :content %>
		<br />
		<%= f.submit "Post" %>
	</p>

<% end %>